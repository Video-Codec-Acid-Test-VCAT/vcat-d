<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:id="@+id/menu_group">
        <item
            android:id="@+id/menu_edit"
            android:title="Edit"
            android:icon="@drawable/ic_edit_playlist"
            android:orderInCategory="0"/>

        <item
            android:id="@+id/menu_play"
            android:title="Play"
            android:icon="@drawable/ic_play_video"
            android:orderInCategory="1"/>

        <!-- Resume, hidden by default -->
        <item
            android:id="@+id/menu_resume"
            android:title="Resume"
            android:icon="@drawable/ic_play_video"
            android:visible="false"
            android:orderInCategory="2"/>

        <item
            android:id="@+id/menu_delete"
            android:title="Delete"
            android:icon="@drawable/ic_trashcan"
            android:orderInCategory="3"/>
    </group>
</menu>
